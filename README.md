Anaerobic Digestion Model Simulator (ADM1)

This is a GUI-based simulation tool for the Anaerobic Digestion Model No. 1 (ADM1).

Install the following dependencies:

bash
pip install numpy scipy pandas matplotlib ttkthemes openpyxl

How to Run
Navigate to the project directory and run:

bash
python app_main.py
